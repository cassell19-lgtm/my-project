# このリポジトリについて

atwiki (<https://w.atwiki.jp/bachelorjpfan/>) で運用していたバチェラー・ジャパン /
バチェロレッテ・ジャパンのファン wiki を、静的サイトに移行したもの。

管理人は1人で、非エンジニア。Windows の cmd で作業している。
説明やコミュニケーションは日本語で行うこと。

現在の状況と残作業は `NEXT_STEPS.md`、使い方は `README.md` にある。**まずそちらを読むこと。**

## 構成

- `content/` — 本文の Markdown。**サイトの実体はここ**
- `src/` — Astro のサイト側 (レイアウト、ルーティング、検索、CSS)
- `src/nav.ts` — サイドバーの構成。ページを増やしたらここに追記が必要
- `public/admin/` — ブラウザ編集画面 (Sveltia CMS)
- `raw/` — atwiki から取得した元 HTML。移行の記録用
- `tools/` — 移行用スクリプト

## 移行で決めたこと（変更するときは理由を踏まえること）

### 表の扱い

Markdown の表は結合セルを表現できない。そのため `tools/convert_to_markdown.py` は、
**1行目が全て `th` かつ結合セルなし**の表だけを Markdown の表にし、それ以外
(縦型の項目表、colspan/rowspan を使う表) は素の HTML として出力している。
この結果、元 wiki の表構造は一切失われていない。表の見た目は CSS 側で統一する。

### URL

atwiki のページ ID (`/pages/27.html`) は意味を持たないので、
`/seasons/bachelor-1/` のような読める URL に変えた。
ID とパスの対応は `tools/convert_to_markdown.py` の `SLUGS` にある。

### 画像を使わない

元 wiki は画像を一切使っておらず、自作の表とテキストのみで構成されている。
これは権利上も安全なので、**この方針は維持すること**。番組のキャプチャ、公式写真、
ロゴを追加してはいけない。出演者は一般人なので肖像権の問題もある。

### 非公開前提の設定

現時点では公開しない方針。`src/layouts/Layout.astro` で `noindex, nofollow` を
送っている。公開する際に外す必要がある (README に記載)。

## 注意点

- **`tools/convert_to_markdown.py` は `content/` を上書きする。** 移行は完了しているので、
  通常は実行しないこと。実行すると移行後の編集が消える。
- **atwiki は bot 風の User-Agent を 403 で弾く。** `tools/fetch_atwiki.py` は
  ブラウザ相当のヘッダを送ることで通している。ここを削らないこと。
- atwiki には一括エクスポート機能が見当たらなかったため、スクレイピングで取得した。
- 開発環境によっては `w.atwiki.jp` への外部アクセスがネットワークポリシーで遮断される。
  その場合は取得済みの `raw/` を使うか、管理人のローカル環境で実行してもらう。

## 作業の進め方

- 変更は `content/` か `src/` に閉じることが多い。`raw/` は触らない
- 変更後は `npm run build` が通ることを確認する
- 管理人は git に不慣れなので、コマンドは cmd にそのまま貼れる形で提示する
